# MVP : 
- all works in userspace
- install/build and launch BeamMP
- .desktop working, for launch only BeamMP and launch the helper
- distro-agnostic install
- detect steam natif or flatpak
- detect if beamng is installed
- detect and install build deps
- detect if vcpkg is installed
- detect if last version of BeamMP is installed
- System check
- TUI

# Future Features :
- gestion of mods ( state, clean, archive, list)
- Fedora atomic works
- can host server BeamMP, in local or lan mode, and config servers